# Story model v0.1

The Story model describes narrative data independently from Reader presentation.
It supports a version `0.1` manifest, narrative and ending nodes, six content
types, optional node effects, and invisible conditional routing nodes.
Conditional nodes contain ordered branches and a required fallback; they never
contain content or effects.
