# Story model v0.1

The Story model describes narrative data independently from Reader presentation.
It currently supports a minimal manifest, narrative nodes, ending nodes, and six
content types. It deliberately contains no Choice, Condition, Effect, state,
memory, route, or persistence fields.
